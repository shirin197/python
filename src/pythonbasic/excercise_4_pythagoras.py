from math import pi

# input = auto user input
r = float(input("Type in the radius of the circle: "))
                                            # Convert r into a string
print ("The area of the circle with radius " + str(r) + " is: " + str(pi * r**2))
